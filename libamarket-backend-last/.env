# Environment Variables Example
# Rename this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Connection
# Use your MongoDB connection string (local or Atlas)
MONGO_URI=mongodb://127.0.0.1:27017/libamarket

# JWT Configuration
JWT_SECRET=yourSuperSecretKeyForJsonWebTokenSignatureChangeThis
JWT_EXPIRE=30d # Token expiration time (e.g., 30d, 1h, 60m)

# Email Configuration (using Mailtrap for development example)
# Replace with your actual email service provider credentials for production (e.g., SendGrid, Mailgun)
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USERNAME=605b5fdba7344b
EMAIL_PASSWORD=EMAIL_PASSWORD=6fd51dc43da128
EMAIL_FROM="LibaMarket Support" <noreply@libamarket.com> # Sender address

# Frontend URL (for CORS and email links)
# Replace with your actual frontend application URL when deployed
FRONTEND_URL=http://localhost:3000 # Example for local Flutter dev

